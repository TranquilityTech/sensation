name: sensation
description: "reliable and lightweight gesture detector"
version: 0.0.1
auther: Ta-Yi Cheng<admin@tranquility.tech>
homepage: https://www.tranquility.tech/

environment:
  sdk: '>=3.2.6 <4.0.0'
  flutter: ">=3.7.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  plugin:
    platforms:
      android:
        package: tech.tranquility.sensation
        pluginClass: Sensation
      ios:
        pluginClass: Sensation